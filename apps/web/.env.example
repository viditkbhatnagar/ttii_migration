WEB_PORT=5173
VITE_API_BASE_URL=http://localhost:4000/api

# SSO — Google client ID (Web app) for the Sign in with Google button on
# the student login page. Leave blank to hide the button. Same value as
# the API's GOOGLE_OAUTH_CLIENT_ID.
VITE_GOOGLE_OAUTH_CLIENT_ID=
